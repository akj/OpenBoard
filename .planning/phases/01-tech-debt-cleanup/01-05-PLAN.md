---
phase: 01-tech-debt-cleanup
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - openboard/exceptions.py
  - openboard/engine/engine_adapter.py
  - openboard/engine/downloader.py
  - openboard/engine/stockfish_manager.py
  - tests/test_exceptions.py
  - tests/test_engine_adapter.py
  - tests/test_downloader.py
  - tests/test_stockfish_manager.py
  - tests/CONCERNS_TRACEABILITY.md
autonomous: true
requirements:
  - TD-11
  - TD-14
tags:
  - exceptions
  - error-handling
  - audit
  - traceability

must_haves:
  truths:
    - "Wired-up exception types (EngineTimeoutError, EngineProcessError, DownloadError, NetworkError) are RAISED at the documented sites in engine_adapter.py / downloader.py / stockfish_manager.py — TD-11 / D-19."
    - "Pruned exception types (AccessibilityError, DialogError, SettingsError, GameStateError, UIError) are removed from openboard/exceptions.py and unimportable — TD-11 / D-19."
    - "tests/test_exceptions.py is updated for the kept set; importing a pruned type raises ImportError — D-20."
    - "tests/CONCERNS_TRACEABILITY.md exists and lists every TD-N → test file → test name with at least 14 entries — TD-14 / D-26 Plan 5 deliverable."
    - "Each TD-N has at least one regression test whose docstring contains the literal 'TD-N' token — D-24."
  artifacts:
    - path: "openboard/exceptions.py"
      provides: "Pruned hierarchy: kept EngineError/EngineNotFoundError/EngineTimeoutError/EngineProcessError/GameError/IllegalMoveError/GameModeError/ConfigurationError/NetworkError/DownloadError; removed AccessibilityError/DialogError/SettingsError/GameStateError/UIError"
      contains: "class DownloadError"
    - path: "openboard/engine/engine_adapter.py"
      provides: "EngineTimeoutError raised on timeout; EngineProcessError raised on startup failure"
      contains: "raise EngineTimeoutError"
    - path: "openboard/engine/downloader.py"
      provides: "DownloadError raise sites for non-security failures (file IO etc.); NetworkError for URL-failures (already added in Plan 04)"
      contains: "raise DownloadError"
    - path: "openboard/engine/stockfish_manager.py"
      provides: "NetworkError bubble-up on download failures (re-raise from Downloader)"
      contains: "NetworkError"
    - path: "tests/test_exceptions.py"
      provides: "Pruned-types-unimportable test + kept-types-raise-correctly tests"
      contains: "test_pruned_exception_types_unimportable"
    - path: "tests/test_engine_adapter.py"
      provides: "TD-11 / D-19 raise-path tests for EngineTimeoutError, EngineProcessError"
      contains: "test_engine_timeout_error_raised"
    - path: "tests/test_downloader.py"
      provides: "TD-11 / D-19 DownloadError / NetworkError raise-path tests for non-security paths"
      contains: "test_network_error_on_url_failure"
    - path: "tests/CONCERNS_TRACEABILITY.md"
      provides: "Audit table mapping every TD-N to its regression test (file + test name + CONCERNS.md item)"
      contains: "TD-01"
  key_links:
    - from: "openboard/engine/engine_adapter.py:get_best_move_async (or similar timeout site)"
      to: "openboard/exceptions.py:EngineTimeoutError"
      via: "raise EngineTimeoutError(operation, timeout_ms)"
      pattern: "raise EngineTimeoutError"
    - from: "openboard/engine/engine_adapter.py:_start_engine (or startup site)"
      to: "openboard/exceptions.py:EngineProcessError"
      via: "raise EngineProcessError(message, return_code, stderr)"
      pattern: "raise EngineProcessError"
    - from: "openboard/engine/stockfish_manager.py:install or similar bubble-up site"
      to: "openboard/exceptions.py:NetworkError"
      via: "re-raise from Downloader call"
      pattern: "raise NetworkError"
    - from: "tests/CONCERNS_TRACEABILITY.md"
      to: "regression tests"
      via: "TD-N row → test file → test function name table"
      pattern: "TD-(0[1-9]|1[0-4])"
---

<objective>
Close out Phase 1 with two deliverables:
1. **TD-11 / D-19 / D-20 — exception hierarchy cleanup:** wire up the five specific exception types that have a logical raise site (EngineTimeoutError, EngineProcessError, DownloadError, NetworkError) in `engine_adapter.py`, `downloader.py`, and `stockfish_manager.py`. Prune the five types that have no clear raise site (AccessibilityError, DialogError, SettingsError, GameStateError, UIError) along with their tests.
2. **TD-14 final audit / D-26 Plan 5 deliverable:** produce `tests/CONCERNS_TRACEABILITY.md` — a single mapping table covering all 14 TD-N requirements, listing for each: the regression test file, the test function name, and the CONCERNS.md item it traces. Cross-check that every TD-N has at least one test whose docstring contains the literal `TD-N` string per D-24.

Purpose: Plan 05 is sequential after Plans 01-04 because it audits the test artifacts those plans produced. Without their SUMMARYs in hand, the traceability table cannot be authored. This is the closing checkpoint.

Output: 1 modified `exceptions.py`, 3 modified engine source files (raise-path additions), 3 modified test files (kept-set assertions + new raise tests), 1 new test file (`test_downloader.py` for non-security DownloadError/NetworkError paths per RESEARCH.md / D-19), 1 new audit doc (`tests/CONCERNS_TRACEABILITY.md`).
</objective>

<execution_context>
@/home/akj/projects/openboard/.claude/get-shit-done/workflows/execute-plan.md
@/home/akj/projects/openboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-tech-debt-cleanup/01-CONTEXT.md
@.planning/phases/01-tech-debt-cleanup/01-RESEARCH.md
@.planning/phases/01-tech-debt-cleanup/01-PATTERNS.md
@.planning/phases/01-tech-debt-cleanup/01-VALIDATION.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

@.planning/phases/01-tech-debt-cleanup/01-01-SUMMARY.md
@.planning/phases/01-tech-debt-cleanup/01-02-SUMMARY.md
@.planning/phases/01-tech-debt-cleanup/01-03-SUMMARY.md
@.planning/phases/01-tech-debt-cleanup/01-04-SUMMARY.md

@openboard/exceptions.py
@openboard/engine/engine_adapter.py
@openboard/engine/downloader.py
@openboard/engine/stockfish_manager.py
@tests/test_exceptions.py
@tests/test_engine_adapter.py
@tests/test_stockfish_manager.py

<interfaces>
<!-- Inherited from Plans 01-04: every prior TD-N fix has at least one regression test. -->
<!-- Plan 04 already raises DownloadError on hash mismatch / ZIP traversal and NetworkError on URL/HTTP failure. -->
<!-- Plan 05 audits + adds remaining D-19 raise sites + produces the traceability doc. -->

From PATTERNS.md §"Shared Patterns" → "Exception class shape":
```python
class EngineTimeoutError(EngineError):
    """Raised when engine operations timeout."""

    def __init__(self, operation: str, timeout_ms: int):
        message = f"Engine {operation} timed out after {timeout_ms}ms"
        super().__init__(message)
        self.operation = operation
        self.timeout_ms = timeout_ms


class EngineProcessError(EngineError):
    """Raised when engine process encounters an error."""

    def __init__(self, message: str, return_code: int | None = None, stderr: str | None = None):
        super().__init__(message)
        self.return_code = return_code
        self.stderr = stderr
```

From CONTEXT.md / RESEARCH.md (D-19) — pruning targets at openboard/exceptions.py:133-160 (approximate range; verify with grep before deleting):
- AccessibilityError
- DialogError
- SettingsError
- GameStateError
- UIError

From CONTEXT.md (D-19) — wire-up targets:
- EngineTimeoutError → raised from EngineAdapter on chess.engine.EngineTerminatedError / asyncio timeout (in get_best_move_async).
- EngineProcessError → raised from EngineAdapter on engine startup failures currently caught as bare RuntimeError.
- DownloadError → raised from Downloader.download_file on IO failures (Plan 04 already wires the security-related raises; Plan 05 picks up any non-security IO failure path).
- NetworkError → raised from StockfishManager on download failures bubbled up.

From RESEARCH.md TD-11 / D-20:
Tests in test_exceptions.py:16-20, 34, 46, 49-51 import and assert the to-be-pruned types — must be UPDATED on prune (not just deleted; some tests will keep but switch to kept types).

From the existing tests/CLAUDE.md and TESTING.md: tests/CONCERNS_TRACEABILITY.md fits the existing pattern (a documentation artifact, not a test module).

From VALIDATION.md row 1-05-05:
```
test -f tests/CONCERNS_TRACEABILITY.md && grep -c "^- TD-" tests/CONCERNS_TRACEABILITY.md   # ≥ 14
```

Expected TD-N → test mapping (from prior plan SUMMARYs — Plan 05 confirms each):
| TD-N | Test file | Test function | CONCERNS.md item |
|------|-----------|---------------|-------------------|
| TD-01 | tests/test_game.py | TestMoveUndoneForwarder::test_move_undone_signal_reconnects_after_new_game | Bug #1 |
| TD-02 | tests/test_chess_controller.py | TestMoveMadePayload::test_move_made_payload_includes_old_board_and_move_kind_for_capture_with_check | (D-02 / Pitfall 1) |
| TD-03 | tests/test_chess_controller.py | TestReplayToPosition::test_replay_to_position_uses_make_move + TestPendingOldBoardRemoved::test_pending_old_board_removed | Bug #2 + Fragile #3 |
| TD-04 | tests/test_chess_controller.py | TestAnnounceAttackingPieces::test_announce_attacking_pieces_includes_pinned_attacker | Bug #3 + Performance #2 |
| TD-05 | tests/test_views_menus.py | TestMenuBindingHygiene::test_no_evt_menu_bound_to_id_any | Bug #4 |
| TD-06 | tests/test_engine_adapter.py | TestSimpleApiRemoved::test_simple_api_removed | Tech Debt — Dead `_simple` API |
| TD-07 | tests/test_game_async.py | TestResolveMoveContextExtracted::test_resolve_move_context_extracted | Tech Debt — Duplicate difficulty resolution |
| TD-08 | tests/test_game.py | TestSyncRequestComputerMoveRemoved::test_request_computer_move_sync_removed | Tech Debt — sync request_computer_move dead |
| TD-09 | tests/test_views_menus.py | TestBookHintAccelerator::test_book_hint_menu_no_accelerator | Tech Debt — `B` shortcut conflict |
| TD-10 | tests/test_game.py | TestPlayerColorRemoved::test_player_color_removed | Tech Debt — player_color attr |
| TD-11 | tests/test_exceptions.py + test_engine_adapter.py + test_downloader.py | (this plan's outputs) | Tech Debt — unused exception types |
| TD-12 | tests/test_paths.py + tests/test_migration.py + tests/test_settings.py | TestPathsModule + TestLegacyPathMigration + TestEnginesDirViaPaths | Security #4 |
| TD-13 | tests/test_chess_controller.py + tests/test_board_state_terminal.py + tests/test_downloader_security.py | TestAnnounceAttackingPieces + TestBoardRefProperty + (security set) | Performance #2/#3 + Security #1/#2/#3 + Fragile #3 |
| TD-14 | this audit | tests/CONCERNS_TRACEABILITY.md | Test Discipline |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — RED tests for prune + raise paths</name>
  <read_first>
    - .planning/phases/01-tech-debt-cleanup/01-VALIDATION.md (rows 1-05-01 through 1-05-05)
    - .planning/phases/01-tech-debt-cleanup/01-CONTEXT.md (D-19, D-20)
    - openboard/exceptions.py (full file — confirm exact line ranges of the 5 to-be-pruned classes)
    - tests/test_exceptions.py (full file — confirm which tests import the pruned types)
    - openboard/engine/engine_adapter.py — find current bare RuntimeError raises that should become EngineTimeoutError / EngineProcessError. Suggested grep: `grep -n "raise RuntimeError" openboard/engine/engine_adapter.py`.
    - openboard/engine/stockfish_manager.py — find current download-failure handling; check whether Downloader exceptions already bubble up or if a try/except converts them.
  </read_first>
  <files>
    tests/test_exceptions.py
    tests/test_engine_adapter.py
    tests/test_downloader.py
    tests/test_stockfish_manager.py
  </files>
  <behavior>
    - Test 1 (TD-11 prune): `test_pruned_exception_types_unimportable` in tests/test_exceptions.py — `with pytest.raises(ImportError): from openboard.exceptions import AccessibilityError`. Repeat for each of {AccessibilityError, DialogError, SettingsError, GameStateError, UIError}. FAILS pre-fix because these are still importable.
    - Test 2 (TD-11 wire-up — EngineTimeoutError): `test_engine_timeout_error_raised` in tests/test_engine_adapter.py — patch the engine to simulate a timeout (e.g., `chess.engine.EngineTerminatedError` or asyncio.TimeoutError); call `get_best_move_async`; assert `EngineTimeoutError` is raised (or surfaced via callback / future). FAILS pre-fix because the current code raises bare RuntimeError.
    - Test 3 (TD-11 wire-up — EngineProcessError): `test_engine_process_error_raised` in tests/test_engine_adapter.py — patch `chess.engine.popen_uci` to raise an OSError or similar startup failure; call `start()`; assert `EngineProcessError` is raised. FAILS pre-fix.
    - Test 4 (TD-11 wire-up — NetworkError on URL failure): `test_network_error_on_url_failure` in tests/test_downloader.py (NEW file per D-19/D-25; security tests already in test_downloader_security.py). Patch `urlopen` to raise `URLError("DNS failure")`; call `download_file`; assert `NetworkError` is raised. NOTE: Plan 04 already wires this — verify it's working; if so, this test PASSES already and is documentation.
    - Test 5 (TD-11 wire-up — DownloadError on OSError): `test_download_error_on_os_error` in tests/test_downloader.py — patch `urlopen` to succeed but file write to fail (mock open); assert `DownloadError`. Plan 04 wires this — verify.
    - Test 6 (TD-14 audit): `test_concerns_traceability_doc_exists` in tests/test_exceptions.py (or somewhere general) — `from pathlib import Path; doc = Path("tests/CONCERNS_TRACEABILITY.md"); assert doc.exists(); content = doc.read_text(); for n in range(1, 15): assert f"TD-{n:02d}" in content, f"TD-{n:02d} must appear in CONCERNS_TRACEABILITY.md"`. FAILS pre-fix because the file does not exist.
  </behavior>
  <action>
    1. **Audit existing `tests/test_exceptions.py`** to find the lines that import and assert the to-be-pruned types:
    ```bash
    grep -n "AccessibilityError\|DialogError\|SettingsError\|GameStateError\|UIError" tests/test_exceptions.py
    ```
    Per RESEARCH.md TD-11 row, lines 16-20, 34, 46, 49-51 are the affected lines. These will be removed in Task 2.

    2. **Append to `tests/test_exceptions.py`** new tests that capture the prune contract:
    ```python
    import importlib

    import pytest


    class TestPrunedExceptionTypes:
        """Verifies TD-11 / D-19: pruned types are unimportable from openboard.exceptions."""

        @pytest.mark.parametrize("type_name", [
            "AccessibilityError",
            "DialogError",
            "SettingsError",
            "GameStateError",
            "UIError",
        ])
        def test_pruned_exception_types_unimportable(self, type_name):
            """Verifies TD-11 / D-19: <type_name> is removed from openboard/exceptions.py.

            Re-imports the module to get a fresh namespace, then asserts the type is absent.
            """
            module = importlib.reload(importlib.import_module("openboard.exceptions"))
            assert not hasattr(module, type_name), (
                f"TD-11 / D-19: {type_name} must be removed from openboard/exceptions.py"
            )


    class TestKeptExceptionTypes:
        """Verifies TD-11 / D-19: wired-up types remain importable and have the expected shape."""

        def test_engine_timeout_error_signature(self):
            """Verifies TD-11 / D-19: EngineTimeoutError(operation, timeout_ms) constructor preserved."""
            from openboard.exceptions import EngineTimeoutError

            error = EngineTimeoutError("get_best_move_async", 5000)
            assert error.operation == "get_best_move_async"
            assert error.timeout_ms == 5000
            assert "5000" in str(error)

        def test_engine_process_error_signature(self):
            """Verifies TD-11 / D-19: EngineProcessError(message, return_code, stderr) constructor preserved."""
            from openboard.exceptions import EngineProcessError

            error = EngineProcessError("startup failed", return_code=1, stderr="bad path")
            assert error.return_code == 1
            assert error.stderr == "bad path"

        def test_download_error_importable(self):
            from openboard.exceptions import DownloadError

            error = DownloadError("https://example.com/x", "io failure")
            assert "x" in str(error)

        def test_network_error_importable(self):
            from openboard.exceptions import NetworkError

            error = NetworkError("dns failed", "no route")
            assert "dns failed" in str(error)
    ```

    3. **Append to `tests/test_engine_adapter.py`** the D-19 wire-up tests:
    ```python
    class TestExceptionWiring:
        """Verifies TD-11 / D-19: EngineAdapter raises EngineTimeoutError and EngineProcessError."""

        def test_engine_timeout_error_raised(self):
            """Verifies TD-11 / D-19: an asyncio timeout in get_best_move_async surfaces as EngineTimeoutError."""
            from unittest.mock import AsyncMock, patch

            from openboard.engine.engine_adapter import EngineAdapter
            from openboard.exceptions import EngineTimeoutError

            # Construct an adapter without auto-starting the engine; patch _engine to raise on play.
            adapter = EngineAdapter(engine_path="/fake/stockfish")
            mock_engine = AsyncMock()
            mock_engine.play.side_effect = TimeoutError("simulated")
            adapter._engine = mock_engine

            # The exact public-API call may be get_best_move_async — adjust to match.
            # If the production timeout-handling code lives in get_best_move_async,
            # the test invokes it and asserts EngineTimeoutError surfaces.
            # Since the async path uses callbacks, the test may need to drive it via
            # a sync wrapper. The minimum assertion: the exception type can be raised.
            with patch.object(adapter, "_run_in_loop", side_effect=EngineTimeoutError("get_best_move_async", 5000)):
                with pytest.raises(EngineTimeoutError):
                    adapter._raise_timeout("get_best_move_async", 5000)   # if such a helper exists
        # NOTE: This test's exact implementation depends on the engine_adapter's structure after
        # Plan 02. Read engine_adapter.py before finalising the assertion strategy. The minimal
        # acceptable form: patch a code path that previously raised RuntimeError and assert it
        # now raises EngineTimeoutError. If that path is hard to drive in a unit test, the
        # signature test in TestKeptExceptionTypes (above) is the fallback proof of D-19.

        def test_engine_process_error_raised_on_startup_failure(self):
            """Verifies TD-11 / D-19: engine startup failure surfaces as EngineProcessError."""
            from unittest.mock import patch

            from openboard.engine.engine_adapter import EngineAdapter
            from openboard.exceptions import EngineProcessError

            adapter = EngineAdapter(engine_path="/fake/that/does/not/exist")
            with patch("openboard.engine.engine_adapter.chess.engine.popen_uci", side_effect=FileNotFoundError("no engine")):
                with pytest.raises((EngineProcessError, RuntimeError)):
                    # If a wrapping refactor lands as part of Task 2, EngineProcessError is raised.
                    # Tolerate RuntimeError on the RED phase.
                    adapter.start()
    ```
    NOTE: this test is intentionally tolerant of the current state. Task 2 will tighten the assertion to `pytest.raises(EngineProcessError)` after the wire-up.

    4. **Create `tests/test_downloader.py`** (NEW file per D-19 — separate from `test_downloader_security.py` which Plan 04 created):
    ```python
    """Tests for openboard/engine/downloader.py — TD-11 / D-19 (non-security raise paths)."""

    from unittest.mock import MagicMock, patch
    from urllib.error import URLError

    import pytest

    from openboard.exceptions import DownloadError, NetworkError


    class TestDownloadFileExceptionWiring:
        """Verifies TD-11 / D-19: download_file raises NetworkError on URL failure, DownloadError on OS failure."""

        def test_network_error_on_url_failure(self, tmp_path):
            """Verifies D-19: a URLError during urlopen is wrapped as NetworkError."""
            from openboard.engine.downloader import StockfishDownloader

            downloader = StockfishDownloader(install_dir=tmp_path)
            with patch("openboard.engine.downloader.urlopen", side_effect=URLError("DNS failure")):
                with pytest.raises(NetworkError):
                    downloader.download_file(url="https://example.com/x", dest_path=tmp_path / "x")

        def test_download_error_on_os_error(self, tmp_path):
            """Verifies D-19: an OSError during file write is wrapped as DownloadError."""
            from openboard.engine.downloader import StockfishDownloader

            downloader = StockfishDownloader(install_dir=tmp_path)

            fake_response = MagicMock()
            fake_response.__enter__.return_value = fake_response
            fake_response.read.side_effect = [b"abc", b""]
            fake_response.headers = {}
            fake_response.getheader.return_value = "3"

            # Patch urlopen to succeed but the open() to fail
            with patch("openboard.engine.downloader.urlopen", return_value=fake_response):
                with patch("builtins.open", side_effect=OSError("disk full")):
                    with pytest.raises(DownloadError):
                        downloader.download_file(
                            url="https://example.com/x",
                            dest_path=tmp_path / "x",
                        )


    class TestConcernsTraceability:
        """Verifies TD-14 / D-26 Plan 5 deliverable: tests/CONCERNS_TRACEABILITY.md exists and is complete."""

        def test_concerns_traceability_doc_exists_and_lists_all_td(self):
            """Verifies TD-14: every TD-01..TD-14 appears in tests/CONCERNS_TRACEABILITY.md."""
            from pathlib import Path

            doc_path = Path("tests/CONCERNS_TRACEABILITY.md")
            assert doc_path.exists(), "TD-14: tests/CONCERNS_TRACEABILITY.md must be created (Plan 5 deliverable)"

            content = doc_path.read_text()
            for td_index in range(1, 15):
                token = f"TD-{td_index:02d}"
                assert token in content, f"TD-14: {token} must be referenced in tests/CONCERNS_TRACEABILITY.md"
    ```

    5. **Append a stockfish_manager NetworkError test to `tests/test_stockfish_manager.py`** (D-19):
    ```python
    class TestStockfishManagerExceptionBubbleUp(unittest.TestCase):
        """Verifies TD-11 / D-19: StockfishManager re-raises NetworkError on download failure."""

        def setUp(self):
            self.temp_dir = tempfile.mkdtemp()

        def test_network_error_bubbles_up_from_downloader(self):
            """Verifies D-19: a NetworkError raised by Downloader propagates to the caller."""
            from openboard.exceptions import NetworkError
            from openboard.engine.stockfish_manager import StockfishManager

            manager = StockfishManager(Path(self.temp_dir))
            with patch.object(manager.downloader, "download_file", side_effect=NetworkError("dns", "no route")):
                with self.assertRaises(NetworkError):
                    # Whatever the public install API is — read stockfish_manager.py to confirm.
                    manager.install_latest()   # adjust to actual method name
    ```

    6. Run all RED tests; confirm they FAIL pre-fix:
    ```bash
    uv run python -m pytest \
      tests/test_exceptions.py::TestPrunedExceptionTypes \
      tests/test_engine_adapter.py::TestExceptionWiring \
      tests/test_downloader.py \
      tests/test_stockfish_manager.py::TestStockfishManagerExceptionBubbleUp \
      --tb=short
    ```
    Expected RED:
    - TestPrunedExceptionTypes — pre-fix the types still exist; assertions fail.
    - TestExceptionWiring::test_engine_process_error_raised_on_startup_failure — currently raises RuntimeError, not EngineProcessError; the tolerant `pytest.raises((EngineProcessError, RuntimeError))` PASSES already (this is intentional — Task 2 tightens the assertion).
    - TestDownloadFileExceptionWiring::test_network_error_on_url_failure — Plan 04's hardening already wires this; should PASS.
    - TestDownloadFileExceptionWiring::test_download_error_on_os_error — Plan 04 wires this; should PASS.
    - TestConcernsTraceability::test_concerns_traceability_doc_exists_and_lists_all_td — file does not exist; FAILS.
    - TestStockfishManagerExceptionBubbleUp — the bubble-up may already work if the manager doesn't catch NetworkError; verify by running.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_exceptions.py::TestPrunedExceptionTypes tests/test_downloader.py::TestConcernsTraceability --tb=short 2>&1 | tail -20</automated>
    <automated>test -f tests/test_downloader.py && echo TEST_DOWNLOADER_NEW</automated>
    <automated>grep -q "test_pruned_exception_types_unimportable" tests/test_exceptions.py && echo PRUNE_TESTS_OK</automated>
    <automated>grep -q "TestExceptionWiring" tests/test_engine_adapter.py && echo WIRING_TESTS_OK</automated>
  </verify>
  <done>
    - All four test files have new tests; the prune tests and the traceability test FAIL pre-fix.
    - The wiring tests for EngineTimeoutError / EngineProcessError are written tolerantly (accept current RuntimeError) so they don't block Task 2 from finalizing the wire-up.
    - The downloader.py raise-path tests pass already (Plan 04 wired them), confirming D-19 is already partially implemented.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prune unused exception types + wire up EngineTimeout/EngineProcess raises (GREEN for TD-11)</name>
  <read_first>
    - openboard/exceptions.py:70-188 (full hierarchy — locate the 5 prunable classes; verify line ranges with grep)
    - openboard/engine/engine_adapter.py — find every `raise RuntimeError(...)` (per RESEARCH.md, the engine startup site at `_start_engine_simple` is gone after Plan 02; the surviving `start()` / `_start_engine` site still likely raises RuntimeError; the timeout site in `get_best_move_async` is the other one). Use:
      ```bash
      grep -n "raise RuntimeError" openboard/engine/engine_adapter.py
      ```
    - openboard/engine/stockfish_manager.py — confirm whether the manager catches Downloader exceptions or lets them propagate
    - tests/test_exceptions.py:16-20, 34, 46, 49-51 — the lines that import / assert pruned types (must update in tandem)
    - .planning/phases/01-tech-debt-cleanup/01-CONTEXT.md (D-19, D-20)
  </read_first>
  <files>
    openboard/exceptions.py
    openboard/engine/engine_adapter.py
    openboard/engine/stockfish_manager.py
    tests/test_exceptions.py
  </files>
  <behavior>
    - All TestPrunedExceptionTypes parametrized cases PASS.
    - All TestKeptExceptionTypes signature tests PASS.
    - The TestExceptionWiring tolerant tests can be tightened to `pytest.raises(EngineTimeoutError)` and `pytest.raises(EngineProcessError)` and still pass.
    - All previously-passing tests still pass after the prune (any test that imported a pruned type has been updated).
  </behavior>
  <action>
    1. **Audit `openboard/exceptions.py`** for the 5 prune targets:
    ```bash
    grep -n "class AccessibilityError\|class DialogError\|class SettingsError\|class GameStateError\|class UIError" openboard/exceptions.py
    ```
    Note: `UIError` and `SettingsError` may have legitimate base-class consumers in the existing hierarchy (e.g., `GameModeError(SettingsError)`). Read the full file before deletion to ensure no `class X(PrunedType)` exists. Per RESEARCH.md the affected ranges are around lines 70-188.

    2. **Delete the 5 prune targets** from `openboard/exceptions.py`. For each:
    - Remove the `class AccessibilityError(...): ...` block (including docstring).
    - Remove the `class DialogError(...): ...` block.
    - Remove `class SettingsError(...): ...` — verify no subclass refers to it; if `GameModeError(SettingsError)` exists, change the parent to `ConfigurationError` (confirmed in CONVENTIONS.md error hierarchy: `GameModeError(ConfigurationError)`).
    - Remove `class GameStateError(...): ...`.
    - Remove `class UIError(...): ...`.
    - After deletion, run:
      ```bash
      grep -n "AccessibilityError\|DialogError\|SettingsError\|GameStateError\|UIError" openboard/
      ```
      Should return zero matches in production code (test files will still reference them — they get cleaned in step 3).

    3. **Update `tests/test_exceptions.py`**:
    - Find the imports of the pruned types (per RESEARCH.md, lines 16-20). DELETE these imports.
    - Find the assertions that test the pruned types (lines 34, 46, 49-51). DELETE the entire test functions/classes that exclusively test pruned types.
    - The TestPrunedExceptionTypes class (added in Task 1) replaces the deleted positive tests with negative-import assertions.

    4. **Wire up `EngineTimeoutError` and `EngineProcessError` in `openboard/engine/engine_adapter.py`**:
    - Run `grep -n "raise RuntimeError" openboard/engine/engine_adapter.py` to find all bare `RuntimeError` raises.
    - For each timeout-related raise (typical pattern: `chess.engine.EngineTerminatedError` or `asyncio.TimeoutError` caught and re-raised), replace with:
      ```python
      raise EngineTimeoutError("get_best_move_async", timeout_ms) from exc
      ```
      where `timeout_ms` is the configured timeout for that operation.
    - For each startup-related raise (typical pattern: caught around `chess.engine.popen_uci`), replace with:
      ```python
      raise EngineProcessError(f"Engine startup failed: {exc}") from exc
      ```
    - Add the imports at the top of `engine_adapter.py` if not already present:
      ```python
      from ..exceptions import EngineTimeoutError, EngineProcessError
      ```
    - Per CONVENTIONS.md "Error Handling" rule: use `raise X(...) from original_exception` to preserve causality.

    5. **Wire up `NetworkError` bubble-up in `openboard/engine/stockfish_manager.py`** (D-19):
    - Find the public install / download API on the manager (e.g., `install_latest()`, `download_stockfish()`). Read the file to confirm the exact name.
    - If the manager currently catches `NetworkError` from the Downloader and converts to a different exception, simply remove the catch — let `NetworkError` propagate. Per Plan 04's Downloader, `NetworkError` and `DownloadError` are already raised directly. This is mostly a "verify nothing is wrapping these" task.
    - Add explicit `from ..exceptions import NetworkError` at the top if any local raise sites are added.

    6. **Tighten the TestExceptionWiring tests in `tests/test_engine_adapter.py`** that Task 1 wrote tolerantly:
    - Change `with pytest.raises((EngineProcessError, RuntimeError)):` to `with pytest.raises(EngineProcessError):`.
    - The exact test plumbing depends on the live structure of `engine_adapter.py`; if a unit test cannot drive the timeout path cleanly, reduce that test to a signature-shape assertion and document the gap in the SUMMARY for Plan 05's traceability doc.

    7. Run the full Plan 05 cohort:
    ```bash
    uv run python -m pytest \
      tests/test_exceptions.py \
      tests/test_engine_adapter.py \
      tests/test_downloader.py \
      tests/test_stockfish_manager.py \
      --tb=short
    ```
    All must pass.

    8. Run the full suite:
    ```bash
    uv run python -m pytest --tb=short
    uv run ruff check openboard/exceptions.py openboard/engine/engine_adapter.py openboard/engine/stockfish_manager.py
    uv run python -m ty check openboard/exceptions.py openboard/engine/engine_adapter.py openboard/engine/stockfish_manager.py
    ```
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_exceptions.py tests/test_engine_adapter.py::TestExceptionWiring tests/test_downloader.py tests/test_stockfish_manager.py --tb=short 2>&1 | tail -15</automated>
    <automated>uv run python -m pytest --tb=short 2>&1 | tail -10</automated>
    <automated>grep -nE "class (AccessibilityError|DialogError|SettingsError|GameStateError|UIError)\b" openboard/exceptions.py | wc -l | grep -q '^0$' && echo PRUNED_CLASSES_GONE</automated>
    <automated>grep -q "raise EngineTimeoutError" openboard/engine/engine_adapter.py && echo ENGINE_TIMEOUT_WIRED</automated>
    <automated>grep -q "raise EngineProcessError" openboard/engine/engine_adapter.py && echo ENGINE_PROCESS_WIRED</automated>
    <automated>grep -nE "(AccessibilityError|DialogError|SettingsError|GameStateError|UIError)" openboard/ -r --include='*.py' 2>&1 | grep -v ':0$' | wc -l | grep -q '^0$' && echo NO_PRUNED_REFS_IN_PRODUCTION</automated>
    <automated>uv run ruff check openboard/exceptions.py openboard/engine/engine_adapter.py openboard/engine/stockfish_manager.py 2>&1 | tail -3</automated>
    <automated>uv run python -m ty check openboard/exceptions.py openboard/engine/engine_adapter.py openboard/engine/stockfish_manager.py 2>&1 | tail -5</automated>
  </verify>
  <done>
    - openboard/exceptions.py contains no `class AccessibilityError|DialogError|SettingsError|GameStateError|UIError` definitions.
    - openboard/engine/engine_adapter.py has at least one `raise EngineTimeoutError` and at least one `raise EngineProcessError`.
    - openboard/engine/stockfish_manager.py either bubbles up NetworkError directly or raises it explicitly.
    - tests/test_exceptions.py imports only the kept set; TestPrunedExceptionTypes parametrized tests all pass.
    - Full pytest suite green; ruff and ty clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author tests/CONCERNS_TRACEABILITY.md (TD-14 final audit deliverable)</name>
  <read_first>
    - All four prior plan SUMMARY files (`01-01-SUMMARY.md` through `01-04-SUMMARY.md`) — they list the TD-N → test mapping each plan produced.
    - .planning/codebase/CONCERNS.md — for the canonical "CONCERNS.md item" labels (e.g., "Bug #1", "Performance #2", "Security #1").
    - .planning/REQUIREMENTS.md §"Tech Debt (TD)" — TD-01..TD-14 specifications.
    - .planning/phases/01-tech-debt-cleanup/01-VALIDATION.md row 1-05-05 (the audit verification command).
    - tests/test_downloader.py::TestConcernsTraceability::test_concerns_traceability_doc_exists_and_lists_all_td (the test that validates this file's existence and TD-N coverage).
  </read_first>
  <files>
    tests/CONCERNS_TRACEABILITY.md
  </files>
  <behavior>
    - tests/CONCERNS_TRACEABILITY.md exists with a markdown table and bullet list mapping every TD-N to its regression test(s).
    - Every TD-01..TD-14 token appears at least once.
    - `grep -c "^- TD-" tests/CONCERNS_TRACEABILITY.md` ≥ 14 (per VALIDATION row 1-05-05).
    - The audit cross-checks: every test docstring referenced in the table contains the literal `TD-N` token per D-24.
  </behavior>
  <action>
    1. **Audit each plan's SUMMARY.md** for the canonical TD-N → test_function_name mapping. The expected mapping (verified by reading SUMMARYs and adjusting if Claude's-Discretion choices changed):
    ```
    TD-01 → tests/test_game.py::TestMoveUndoneForwarder::test_move_undone_signal_reconnects_after_new_game (CONCERNS: Bug #1)
    TD-02 → tests/test_chess_controller.py::TestMoveMadePayload::test_move_made_payload_includes_old_board_and_move_kind_for_capture_with_check
            tests/test_chess_controller.py::TestMoveMadePayload::test_move_made_payload_quiet_move
            tests/test_chess_controller.py::TestMoveMadePayload::test_move_kind_check_post_push (CONCERNS: D-02 / Pitfall 1)
    TD-03 → tests/test_chess_controller.py::TestReplayToPosition::test_replay_to_position_uses_make_move
            tests/test_chess_controller.py::TestPendingOldBoardRemoved::test_pending_old_board_removed (CONCERNS: Bug #2 + Fragile #3)
    TD-04 → tests/test_chess_controller.py::TestAnnounceAttackingPieces::test_announce_attacking_pieces_includes_pinned_attacker (CONCERNS: Bug #3 + Performance #2)
    TD-05 → tests/test_views_menus.py::TestMenuBindingHygiene::test_no_evt_menu_bound_to_id_any (CONCERNS: Bug #4)
    TD-06 → tests/test_engine_adapter.py::TestSimpleApiRemoved::test_simple_api_removed (CONCERNS: Tech Debt — Dead `_simple` API)
    TD-07 → tests/test_game_async.py::TestResolveMoveContextExtracted::test_resolve_move_context_extracted (CONCERNS: Tech Debt — Duplicate difficulty resolution)
    TD-08 → tests/test_game.py::TestSyncRequestComputerMoveRemoved::test_request_computer_move_sync_removed (CONCERNS: Tech Debt — sync request_computer_move dead)
    TD-09 → tests/test_views_menus.py::TestBookHintAccelerator::test_book_hint_menu_no_accelerator (CONCERNS: Tech Debt — `B` shortcut conflict)
    TD-10 → tests/test_game.py::TestPlayerColorRemoved::test_player_color_removed (CONCERNS: Tech Debt — player_color attr)
    TD-11 → tests/test_exceptions.py::TestPrunedExceptionTypes::*
            tests/test_exceptions.py::TestKeptExceptionTypes::*
            tests/test_engine_adapter.py::TestExceptionWiring::*
            tests/test_downloader.py::TestDownloadFileExceptionWiring::*
            tests/test_stockfish_manager.py::TestStockfishManagerExceptionBubbleUp::* (CONCERNS: Tech Debt — unused exception types)
    TD-12 → tests/test_paths.py::TestPathsModule::*
            tests/test_migration.py::TestLegacyPathMigration::*
            tests/test_settings.py::TestEnginesDirViaPaths::test_engines_dir_resolves_via_paths (CONCERNS: Security #4)
    TD-13 → tests/test_chess_controller.py::TestAnnounceAttackingPieces (Performance #2)
            tests/test_board_state_terminal.py::TestBoardRefProperty (Performance #3)
            tests/test_downloader_security.py::TestSSLContext (Security #3)
            tests/test_downloader_security.py::TestSHA256Verification (Security #1)
            tests/test_downloader_security.py::TestZipPathTraversalGuard (Security #2)
            tests/test_chess_controller.py::TestPendingOldBoardRemoved (Fragile #3)
    TD-14 → tests/CONCERNS_TRACEABILITY.md (this audit; D-26 Plan 5 deliverable)
    ```

    2. **Cross-check D-24 docstring discipline:** for each test function in the mapping, `grep -A 5 "def test_<name>" <file> | grep "TD-XX"` must find the token. If any test docstring is missing the token, ADD it via a small edit to that test file in this task.

    3. **Author `tests/CONCERNS_TRACEABILITY.md`** with this exact structure:
    ```markdown
    # Phase 1 — CONCERNS Traceability

    **Audit date:** <today>
    **Phase:** 01-tech-debt-cleanup
    **Total TD requirements:** 14 (TD-01..TD-14)

    Every regression test below has a docstring containing the literal `TD-N` token per
    CONTEXT.md D-24. This file is verified by
    `tests/test_downloader.py::TestConcernsTraceability::test_concerns_traceability_doc_exists_and_lists_all_td`.

    ## Mapping Table

    | TD-N | CONCERNS.md Item | Test File | Test Class::Function |
    |------|-------------------|-----------|----------------------|
    | TD-01 | Bug #1: move_undone signal lost after new_game() | tests/test_game.py | TestMoveUndoneForwarder::test_move_undone_signal_reconnects_after_new_game |
    | TD-02 | D-02 / Pitfall 1 — MoveKind from POST-push board state | tests/test_chess_controller.py | TestMoveMadePayload::test_move_made_payload_includes_old_board_and_move_kind_for_capture_with_check, ::test_move_made_payload_quiet_move, ::test_move_kind_check_post_push |
    | TD-03 | Bug #2 + Fragile #3 — _navigate_to_position bypass + _pending_old_board state | tests/test_chess_controller.py | TestReplayToPosition::test_replay_to_position_uses_make_move, ::test_replay_to_position_clamps_out_of_range; TestPendingOldBoardRemoved::test_pending_old_board_removed |
    | TD-04 | Bug #3 + Performance #2 — pinned attackers / O(64×legal_moves) | tests/test_chess_controller.py | TestAnnounceAttackingPieces::test_announce_attacking_pieces_includes_pinned_attacker |
    | TD-05 | Bug #4 — wx.ID_ANY menu bind fallback | tests/test_views_menus.py | TestMenuBindingHygiene::test_no_evt_menu_bound_to_id_any |
    | TD-06 | Tech Debt — Dead `_simple` API surface | tests/test_engine_adapter.py | TestSimpleApiRemoved::test_simple_api_removed |
    | TD-07 | Tech Debt — Duplicate difficulty-resolution logic | tests/test_game_async.py | TestResolveMoveContextExtracted::test_resolve_move_context_extracted |
    | TD-08 | Tech Debt — Sync request_computer_move dead in production | tests/test_game.py | TestSyncRequestComputerMoveRemoved::test_request_computer_move_sync_removed (+ migrated tests in test_game_opening_book_integration.py, test_chess_controller_opening_book.py, test_game_mode.py) |
    | TD-09 | Tech Debt — `B` keyboard shortcut conflict | tests/test_views_menus.py | TestBookHintAccelerator::test_book_hint_menu_no_accelerator, ::test_request_book_hint_keyaction_still_defined |
    | TD-10 | Tech Debt — player_color backward-compat attr | tests/test_game.py | TestPlayerColorRemoved::test_player_color_removed |
    | TD-11 | Tech Debt — Unused exception types | tests/test_exceptions.py | TestPrunedExceptionTypes::test_pruned_exception_types_unimportable[*]; TestKeptExceptionTypes::* |
    |       | + raise wiring | tests/test_engine_adapter.py | TestExceptionWiring::test_engine_timeout_error_raised, ::test_engine_process_error_raised_on_startup_failure |
    |       | + raise wiring | tests/test_downloader.py | TestDownloadFileExceptionWiring::test_network_error_on_url_failure, ::test_download_error_on_os_error |
    |       | + raise wiring | tests/test_stockfish_manager.py | TestStockfishManagerExceptionBubbleUp::test_network_error_bubbles_up_from_downloader |
    | TD-12 | Security #4 — cwd-relative persistence paths | tests/test_paths.py | TestPathsModule::test_paths_resolve_under_override, ::test_paths_directories_are_created, ::test_paths_default_resolves_via_platformdirs |
    |       | + migration | tests/test_migration.py | TestLegacyPathMigration::test_one_shot_migration_moves_legacy_files, ::test_migration_idempotent, ::test_migration_no_overwrite |
    |       | + settings switch | tests/test_settings.py | TestEnginesDirViaPaths::test_engines_dir_resolves_via_paths |
    | TD-13 | Performance #2 — pinned attackers (also TD-04) | tests/test_chess_controller.py | TestAnnounceAttackingPieces |
    |       | Performance #3 — BoardState.board copy-per-call | tests/test_board_state_terminal.py | TestBoardRefProperty::test_board_ref_returns_live_reference, ::test_board_property_still_returns_copy |
    |       | Security #1 — SHA-256 verification | tests/test_downloader_security.py | TestSHA256Verification::test_download_sha256_mismatch_raises, ::test_download_sha256_match_succeeds, ::test_download_warns_when_no_sha256 |
    |       | Security #2 — ZIP path traversal | tests/test_downloader_security.py | TestZipPathTraversalGuard::test_extract_zip_rejects_path_traversal, ::test_extract_zip_accepts_safe_archive |
    |       | Security #3 — explicit SSL context | tests/test_downloader_security.py | TestSSLContext::test_download_uses_ssl_context |
    |       | Fragile #3 — _pending_old_board (also TD-03) | tests/test_chess_controller.py | TestPendingOldBoardRemoved |
    | TD-14 | Test discipline final audit | tests/CONCERNS_TRACEABILITY.md (this file) | tests/test_downloader.py::TestConcernsTraceability::test_concerns_traceability_doc_exists_and_lists_all_td |

    ## TD Coverage Bullet List

    Every TD requirement has at least one regression test as required by D-23 / D-24:

    - TD-01: tests/test_game.py::TestMoveUndoneForwarder
    - TD-02: tests/test_chess_controller.py::TestMoveMadePayload
    - TD-03: tests/test_chess_controller.py::TestReplayToPosition + TestPendingOldBoardRemoved
    - TD-04: tests/test_chess_controller.py::TestAnnounceAttackingPieces
    - TD-05: tests/test_views_menus.py::TestMenuBindingHygiene
    - TD-06: tests/test_engine_adapter.py::TestSimpleApiRemoved
    - TD-07: tests/test_game_async.py::TestResolveMoveContextExtracted
    - TD-08: tests/test_game.py::TestSyncRequestComputerMoveRemoved
    - TD-09: tests/test_views_menus.py::TestBookHintAccelerator
    - TD-10: tests/test_game.py::TestPlayerColorRemoved
    - TD-11: tests/test_exceptions.py::TestPrunedExceptionTypes + TestKeptExceptionTypes; tests/test_engine_adapter.py::TestExceptionWiring; tests/test_downloader.py::TestDownloadFileExceptionWiring; tests/test_stockfish_manager.py::TestStockfishManagerExceptionBubbleUp
    - TD-12: tests/test_paths.py + tests/test_migration.py + tests/test_settings.py::TestEnginesDirViaPaths
    - TD-13: tests/test_board_state_terminal.py::TestBoardRefProperty + tests/test_downloader_security.py (3 classes) + (TD-04, TD-03 cross-references)
    - TD-14: tests/CONCERNS_TRACEABILITY.md (this file)

    ## CONCERNS.md → Phase 1 Disposition

    | CONCERNS.md Item | Disposition | Where Addressed |
    |------------------|-------------|------------------|
    | Bug #1 (move_undone lost after new_game) | FIXED | TD-01 (Plan 01) |
    | Bug #2 (_navigate_to_position bypasses model) | FIXED | TD-03 (Plan 01) |
    | Bug #3 (announce_attacking_pieces misses pinned attackers) | FIXED | TD-04 (Plan 03) |
    | Bug #4 (wx.ID_ANY menu fallthrough) | FIXED | TD-05 (Plan 03) |
    | Performance #1 (O(n) board replay in announce) | ELIMINATED | Side effect of TD-02 (Plan 01) |
    | Performance #2 (O(64 × legal_moves) attackers) | ELIMINATED | TD-04 (Plan 03) |
    | Performance #3 (BoardState.board copy per access) | FIXED | TD-13 board_ref slice (Plan 03) |
    | Fragile #1 (EngineAdapter overall thread/async refactor) | DEFERRED | Out of v1 scope; CONTEXT.md §Deferred |
    | Fragile #2 (Computer-move announcement double-path) | DEFERRED (reassess) | After D-02/D-03; flagged in CONTEXT.md §Deferred |
    | Fragile #3 (_pending_old_board mutable state) | ELIMINATED | Side effect of TD-02/TD-03 (Plan 01) |
    | Fragile #4 (wxPython Linux index pinned to ubuntu-24.04) | DEFERRED | Operational/build concern; out of v1 |
    | Scaling #1 (CvC pause/stop button) | DEFERRED | UX feature; own phase |
    | Security #1 (SHA-256 verification) | FIXED | TD-13 / D-21 (Plan 04); WARNING fallback per Pitfall 5 |
    | Security #2 (ZIP path traversal) | FIXED | TD-13 / D-21 (Plan 04) |
    | Security #3 (Explicit SSL context) | FIXED | TD-13 / D-21 (Plan 04) |
    | Security #4 (cwd-relative persistence) | FIXED | TD-12 / D-09/D-10/D-11 (Plan 04) |
    | Deps at Risk #1 (accessible-output3 git pin) | DEFERRED | v1.x effort |
    | Missing Critical #1 (promotion-piece UI) | DEFERRED | New capability; own phase |
    | Missing Critical #2 (persistence between sessions) | DEFERRED | Phase 2a + Phase 4b |
    | Test Gaps #1-#4 | PARTIAL | TD-14 covers narrow regression tests; broader view-layer coverage opportunistic |

    ## Validation

    Per VALIDATION.md row 1-05-05, this file is verified by:

    ```bash
    test -f tests/CONCERNS_TRACEABILITY.md
    grep -c "^- TD-" tests/CONCERNS_TRACEABILITY.md   # ≥ 14
    ```

    ---

    *Audit produced by Plan 05 (D-26 Plan 5 deliverable) on completion of Phase 1.*
    ```

    4. Verify the audit assertion:
    ```bash
    test -f tests/CONCERNS_TRACEABILITY.md
    grep -c "^- TD-" tests/CONCERNS_TRACEABILITY.md   # must be >= 14
    ```

    5. Run the traceability validation test:
    ```bash
    uv run python -m pytest tests/test_downloader.py::TestConcernsTraceability --tb=short
    ```
    Must PASS now.

    6. **Cross-check D-24 docstring discipline.** For each TD-N test referenced in the table, verify the test docstring contains the literal `TD-N` token. Run:
    ```bash
    for n in 01 02 03 04 05 06 07 08 09 10 11 12 13 14; do
      grep -lr "TD-${n}" tests/ --include='*.py' 2>/dev/null | head -1 | xargs -I{} echo "TD-${n}: {}"
    done
    ```
    Every TD-N must show at least one test file. If any TD-N is missing, ADD the docstring tag to the relevant test in this task before commit.

    7. Final full-suite green run:
    ```bash
    uv run python -m pytest --tb=short
    uv run ruff check .
    uv run python -m ty check
    ```
  </action>
  <verify>
    <automated>test -f tests/CONCERNS_TRACEABILITY.md && echo CONCERNS_DOC_EXISTS</automated>
    <automated>grep -c "^- TD-" tests/CONCERNS_TRACEABILITY.md | awk '{exit ($1 < 14)}' && echo TD_COUNT_OK</automated>
    <automated>for n in 01 02 03 04 05 06 07 08 09 10 11 12 13 14; do grep -q "TD-${n}" tests/CONCERNS_TRACEABILITY.md || echo MISSING_TD-${n}; done</automated>
    <automated>uv run python -m pytest tests/test_downloader.py::TestConcernsTraceability --tb=short 2>&1 | tail -5</automated>
    <automated>for n in 01 02 03 04 05 06 07 08 09 10 11 12 13 14; do hits=$(grep -lr "TD-${n}" tests/ --include='*.py' 2>/dev/null | head -1); if [ -z "$hits" ]; then echo "TD-${n}: MISSING DOCSTRING"; fi; done</automated>
    <automated>uv run python -m pytest --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>
    - tests/CONCERNS_TRACEABILITY.md exists with the mapping table and bullet list.
    - `grep -c "^- TD-" tests/CONCERNS_TRACEABILITY.md` returns ≥ 14.
    - Every TD-01..TD-14 token appears in the file at least once.
    - `tests/test_downloader.py::TestConcernsTraceability::test_concerns_traceability_doc_exists_and_lists_all_td` passes.
    - Every TD-N has at least one test docstring referencing it (D-24 audit).
    - Full pytest suite green; ruff and ty clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new boundaries) | Plan 05 is exception-hierarchy cleanup + audit doc. The new `raise EngineTimeoutError / EngineProcessError / NetworkError` sites do not add boundaries — they replace existing bare-RuntimeError raises. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Repudiation | Bare RuntimeError obscures the cause of engine timeouts and startup failures | mitigate | TD-11 / D-19: typed exceptions (`EngineTimeoutError`, `EngineProcessError`) carry operation/timeout/return_code context. Improves auditability and diagnostic logging. Verified by `tests/test_engine_adapter.py::TestExceptionWiring`. |
| T-05-02 | Information Disclosure | Pruned exception types might leak internal state via subclass introspection | accept | The pruned types had no callers and no raise sites — pruning REDUCES surface area. No information disclosure introduced. |

**ASVS L1 mapping:** No new categories activated. Plan 05 augments V14 (Configuration) traceability via the `tests/CONCERNS_TRACEABILITY.md` audit doc — every TD-N requirement is now traceable to its regression test.
</threat_model>

<verification>
```bash
# Plan 05 cohort
uv run python -m pytest \
  tests/test_exceptions.py \
  tests/test_engine_adapter.py::TestExceptionWiring \
  tests/test_downloader.py \
  tests/test_stockfish_manager.py \
  --tb=short

# Greppable invariants
grep -nE "class (AccessibilityError|DialogError|SettingsError|GameStateError|UIError)\b" openboard/exceptions.py | wc -l    # 0
grep -nE "(AccessibilityError|DialogError|SettingsError|GameStateError|UIError)" openboard/ -r --include='*.py' | wc -l    # 0
grep -q "raise EngineTimeoutError" openboard/engine/engine_adapter.py
grep -q "raise EngineProcessError" openboard/engine/engine_adapter.py
test -f tests/CONCERNS_TRACEABILITY.md
grep -c "^- TD-" tests/CONCERNS_TRACEABILITY.md    # ≥ 14

# D-24 docstring discipline (every TD-N referenced in at least one test)
for n in 01 02 03 04 05 06 07 08 09 10 11 12 13 14; do
  grep -lr "TD-${n}" tests/ --include='*.py' >/dev/null || echo "MISSING TD-${n}"
done

# Full regression
uv run python -m pytest --tb=short
uv run ruff check .
uv run python -m ty check
```
</verification>

<success_criteria>
- TD-11 (prune): `AccessibilityError`, `DialogError`, `SettingsError`, `GameStateError`, `UIError` are removed from `openboard/exceptions.py`; `tests/test_exceptions.py::TestPrunedExceptionTypes` passes (importing each raises ImportError).
- TD-11 (wire-up): `EngineAdapter` raises `EngineTimeoutError` and `EngineProcessError` at the documented sites; `Downloader` raises `DownloadError` / `NetworkError` (Plan 04 already wired the security paths; Plan 05 confirms non-security paths); `StockfishManager` lets `NetworkError` propagate.
- TD-14 (audit): `tests/CONCERNS_TRACEABILITY.md` exists, lists every TD-01..TD-14 with at least one regression test, and is verified by `tests/test_downloader.py::TestConcernsTraceability::test_concerns_traceability_doc_exists_and_lists_all_td`.
- D-24 docstring discipline: every TD-N has at least one test whose docstring contains the literal `TD-N` token.
- Full pytest suite green; `uv run ruff check .` and `uv run python -m ty check` clean.
</success_criteria>

<output>
Create `.planning/phases/01-tech-debt-cleanup/01-05-SUMMARY.md` documenting:
- Final list of pruned exception classes (with line ranges before deletion).
- Final list of `raise <SpecificError>` sites added in `engine_adapter.py` and `stockfish_manager.py` (with file:line).
- The TD-14 audit verdict: every TD-N has a regression test.
- Any TD-N where the test was relaxed (e.g., a `pytest.raises((EngineProcessError, RuntimeError))` tolerance left in place because the timeout path was hard to drive in a unit test) — explicitly enumerated for future tightening.
- Confirmation that `tests/CONCERNS_TRACEABILITY.md` is committed and validated.
- A pointer for `/gsd-verify-work` orchestrator: "Phase 1 closes here; the verifier should run the full Plan 01-05 cohort plus `tests/test_downloader.py::TestConcernsTraceability` as the gate."
</output>
